//publisher = client.publish(userid, {
//  message:"<%=j render 'newentry', :desc => @tiledesc, :phere => @playershere %>"
//});
$("#q").val("");
scrollTo_val = $('.panel-body').prop('scrollHeight')+10 + 'px';
$('.panel-body').slimScroll({
  scrollTo: scrollTo_val
});
